# Agentic habits: Claude instructions

@AGENTS.md

Claude-specific note: this file is project context, not enforcement. What is
actually enforced here is `.github/checks.sh`, `.github/test-gate.sh`,
`.github/test-install.sh` and the CI workflow. A rule in none of them is a hint,
which is the argument the package itself makes.
